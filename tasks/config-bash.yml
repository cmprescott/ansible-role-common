---
# All configuration tasks file for ansible-role-common

- name: config |  bash | ensure ~/.bash_profile
  template:
    src: bash/bash_profile.j2
    dest: ~/.bash_profile
    
- name: config |  bash | ensure ~/.bashrc
  template:
    src: bash/bashrc.j2
    dest: ~/.bashrc

- name: config |  bash | ensure ~/.bash_aliases
  template:
    src: bash/bash_aliases.j2
    dest: ~/.bash_aliases

- name: config |  bash | ensure no ~/.profile
  file:
    path: ~/.profile
    state: absent

- name: config |  bash | ensure ~/.dircolors
  copy:
    src: dircolors
    dest: ~/.dircolors
